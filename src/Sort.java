import java.util.ArrayList;

public class Sort {
    public ArrayList<Cloud> list;
 public void Sort(ArrayList<Cloud> list){

 }
 public ArrayList<Cloud> getSortedListHigh(){
     int b,k,tmp;
     for (k=1; k < zahlen; k++){
         for(b = 0; b < (zahlen-k); b++){
             if(liste[b] > liste[b+1]) {
                 tmp=liste[b+1];
                 liste[b+1]=liste[b];
                 liste[b]=tmp;
             }
         }
     }
     return
 }



}
